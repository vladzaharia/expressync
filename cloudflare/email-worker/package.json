{
  "name": "polaris-email-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Email Worker for Polaris Express — receives HMAC-signed POST /send from the Fresh app and forwards to Cloudflare Email Service.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:dry-run": "wrangler deploy --dry-run",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250901.0",
    "typescript": "^5.6.0",
    "wrangler": "^4.0.0"
  }
}
